{
  "name": "avi-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "smoke:local": "cross-env TEST_ENV=local playwright test --project=chromium --grep \"@smoke\"",
    "smoke:production": "cross-env TEST_ENV=production playwright test --project=chromium --grep \"@smoke\"",
    "ci": "npm run typecheck && npm run test:api && npm run smoke:production",
    "test": "playwright test",
    "typecheck": "tsc --noEmit",
    "test:smoke": "playwright test --grep \"@smoke\"",
    "test:regression": "playwright test --grep \"@regression\"",
    "test:api": "playwright test --project=api",
    "test:chromium": "playwright test --project=chromium",
    "test:integration": "playwright test --project=chromium --grep \"@integration\"",
    "report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@types/node": "^26.1.2",
    "cross-env": "^10.1.0",
    "http-server": "^14.1.1",
    "typescript": "^7.0.2"
  }
}
